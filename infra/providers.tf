# The AzureRM provider connects Terraform to your Azure subscription.
# Authentication is handled via the Azure CLI ("az login") or environment variables.
# See: https://registry.terraform.io/providers/hashicorp/azurerm/latest/docs
provider "azurerm" {
  features {}
}
